apiVersion: v1
kind: Service
metadata:
  name: sovereign-service
spec:
  type: NodePort
  selector:
    app: sovereign-ops
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30008
